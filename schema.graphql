  
type Movie {
  id: Int!
  title: String!
  rating: Float!
  summary: String!
  language: String!
  medium_cover_image: String!
}
type Query {
    hello:String!
    movies(limit:Int, rating:Float): [Movie!]!

}
#이게 restapi에서 api 의 느낌임 가능한 요청을 모두 모아논 다나와 같은 느낌임
